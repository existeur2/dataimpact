/**
 * TODO:
 * * Display user list with all its informations
 * * Add a new row at the biginning of the table containing average age, male/female ratio, most occured job type, most occured job title and average salary
 * * Add a filter to filter by keyword
 * * Add a filter to filter by salary or age range
 */
const Table = () => {
  return <div></div>;
};

export default Table;
